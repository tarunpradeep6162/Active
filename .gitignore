node_modules
dist
*.log
.DS_Store
# QA captures (reference frames are third-party imagery — never commit or ship)
qa/out/
qa/cache/
sheet.html

# Private birthday material — never commit; encrypt with `npm run vault` instead
birthday-private/
